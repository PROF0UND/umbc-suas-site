---
name: 'TODO: Vehicle Name'
year: 2026
status: active
specs:
  wingspan: 'TODO: e.g. 96 in'
  MTOW: 'TODO: e.g. 20 lb'
  endurance: 'TODO: e.g. 15 min'
  propulsion: 'TODO: e.g. electric pusher prop'
---

TODO: Replace this placeholder with a real vehicle. This body is rendered on the vehicle's
detail page — use it for design-decision documentation (why the team chose this
configuration, with images).

Optional frontmatter fields not set here: `heroImage`, `gallery` (array of image paths),
and `docsUrl` (link to this vehicle's page on the documentation site once it exists —
leave unset and the page links to `/docs` instead).

Retired vehicles should keep `status: retired` rather than being deleted — the archive is
part of the competition deliverable.
